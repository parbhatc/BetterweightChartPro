/** @typedef {object} PivotLevelsSettings */
/** @typedef {object} LiquidityXSettings */

/** JSDoc typedef holder — settings shapes live in pivot/liquidity modules. */
export class SettingsSchema {}
