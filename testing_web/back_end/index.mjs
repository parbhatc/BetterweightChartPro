/**
 * @deprecated Testing is served from the main app at http://localhost:3460/testing/
 * Run: npm run dev
 */
console.error("Testing site moved to http://localhost:3460/testing/ — use: npm run dev");
process.exit(1);
